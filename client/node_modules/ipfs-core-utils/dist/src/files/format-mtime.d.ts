export = formatMtime;
/**
 * @param {MTime} mtime
 * @returns {string}
 */
declare function formatMtime(mtime: MTime): string;
declare namespace formatMtime {
    export { MTime };
}
type MTime = {
    /**
     * - the number of seconds since (positive) or before
     * (negative) the Unix Epoch began
     */
    secs: number;
    /**
     * - the number of nanoseconds since the last full
     * second.
     */
    nsecs: number;
};
//# sourceMappingURL=format-mtime.d.ts.map